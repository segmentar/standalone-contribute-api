using Core;

namespace Core
{
    using System;

    public partial class Contribute
    {
        public static String SolidRenderErrorMessage()
        {
            String stringResult = default;

            var descriptor = String.Empty;
            
            descriptor = descriptor + "This object just so happned to be default" + ' ' + ":(";

            var result = descriptor;

            stringResult = result;

            return stringResult;
        }
    }
}
